import 'package:flutter/material.dart';

class SettingsLineButton extends StatelessWidget {
  const SettingsLineButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
