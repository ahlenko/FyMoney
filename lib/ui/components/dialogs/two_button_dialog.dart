import 'package:flutter/material.dart';

class TwoButtonDialog extends StatelessWidget {
  const TwoButtonDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
